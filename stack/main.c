#include <stdlib.h>
#include <stdio.h>
#include "stack.h"

int main (){
    // int size;
    // stackNode * stack = NULL;
    // stackNode * teste = NULL;
    // scanf("%d\n",&size);
    // for(int i = 0; i < size;i++){
    //     int elemento;
    //     scanf("%d",&elemento);
    //     push(&stack,elemento);
    // }
    return 0;
}